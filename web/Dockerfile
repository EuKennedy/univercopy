# Frontend — por enquanto serve o protótipo (localStorage).
# Na Sprint 1, migrar para React/Next consumindo a API (VITE_API_URL).
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
