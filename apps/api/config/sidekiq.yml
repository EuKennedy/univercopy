:concurrency: <%= ENV.fetch("SIDEKIQ_CONCURRENCY") { 10 } %>
:queues:
  - [critical, 3]
  - [ai, 2]
  - [default, 1]
  - [mailers, 1]
  - [imports, 1]

# Crons populados nas Fases 5+6 (cleanup tokens, reconcile cost, sync drift).
:schedule:
  # cleanup_expired_tokens:
  #   cron: "0 3 * * *"
  #   class: CleanupExpiredTokensJob
  #   queue: default
